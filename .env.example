# 环境变量模板 — 复制为 .env 并填入真实值
# .env 已在 .gitignore 中，不会提交到 Git

# DashScope 大模型 API Key (必需)
DASHSCOPE_API_KEY=your_api_key_here

# 服务端口 (可选，默认 8080)
SERVER_PORT=8080

# Spring 激活 profile (可选，不设置则使用默认配置)
# SPRING_PROFILES_ACTIVE=dev
