# ============================================================
# 项目环境变量说明
# ============================================================
# 本项目为多模块结构，每个模块在 src/main/resources/ 下有
# 独立的 .env 文件，仅包含该模块需要的环境变量。
#
# 放置位置：
#   agent-core/src/main/resources/.env    → Agent Core 模块
#   mcp-server/src/main/resources/.env    → MCP Server 模块
#
# 每个子模块目录下都有 .env.example 模板文件，
# 复制为 src/main/resources/.env 并填入真实值即可。
#
# 加载方式：每个模块的 application.yml 中通过
#   spring.config.import: optional:classpath:.env[.properties]
# 自动加载 .env 文件。
#
# .env 已通过 .gitignore 全局忽略，不会提交到 Git。
# ============================================================
